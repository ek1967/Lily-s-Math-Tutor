# הנחיות לעבודה על הריפו הזה

אפליקציית לימוד מתמטיקה בעברית (RTL), אתר סטטי על GitHub Pages, ללא שרת.
התוכנית המלאה נמצאת בהיסטוריית התכנון; מה שלמטה הוא הכללים שקל לשבור בטעות.

## כללים שלא נשברים

- **בטיחות דו־כיוונית**: אף שדה טקסט לא מכיל גם עברית וגם LaTeX. תמיד זוג `{ he, tex }`.
  מתמטיקה מרונדרת רק דרך `MathInline` / `MathBlock`, שמבודדים `dir="ltr"`.
- **אין אדום בממשק.** שגיאה מקבלת `almost` (ענבר). המילים הן "כמעט" / "בואי ננסה שוב",
  לעולם לא "לא נכון". אין X, אין צליל שגיאה.
- **לשון נקבה** בכל טקסט שהמשתמשת רואה.
- **אין תוויות כיתה בממשק.** `Topic.grade` הוא לוגיקה פנימית בלבד.
- **Tailwind לוגי בלבד**: `ms/me/ps/pe/start/end/text-start/text-end`.
  אף פעם לא `ml/mr/pl/pr/left/right/text-left/text-right`.
- **גנרטורים חייבים להיות טהורים**: בלי `Math.random`, בלי `Date.now`, בלי `new Date()`.
  אותו seed ⇒ אותו תרגיל, תמיד.
- **`TopicId` קבוע לנצח.** הוא מפתח הקישור להיסטוריה השמורה. כותרות אפשר לשנות חופשי.
- **אסור לאנדקס `boolean` ב-IndexedDB.** לכן `Attempt.correct` הוא `0 | 1`.
- **גישה ל-DB רק דרך `lib/db/repos/*`.** אף רכיב לא מייבא `db` ישירות.
- **גרסאות סכימה תוספתיות בלבד.** בלוק `version()` ששוחרר לעולם לא נערך.
- **תאריכים דרך `dayKey()`** באזור Asia/Jerusalem, לא UTC.
- **`base` ב-`vite.config.ts`** חייב להישאר `/Lily-s-Math-Tutor/` בבנייה.
- **בלי CDN.** ה-CSP חוסם הכול חוץ מ-`api.anthropic.com`. גופנים ונכסים מקומיים בלבד.

## אחרי כל שינוי

```bash
npm run typecheck && npm test -- --run && npm run build
```

## מבנה

`src/types` טיפוסים · `src/data` תוכנית לימודים ותוכן הוראה · `src/generators` מנוע התרגילים ·
`src/lib` DB, חזרות מרווחות, AI, קבצים · `src/components` רכיבים · `src/features` פיצ'רים ·
`src/routes` עטיפות נתיב דקות.
